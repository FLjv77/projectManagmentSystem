export enum ProjectRemoveMarginalization {
  health, culture, Construction
}
export enum ProjectRuralWay {
  way, repair
}
export enum WayType {
  asphalt, concrete, soil
}
export enum ProjectRuralWater {
  well, fountain, dam, distributionNetwork, source, pomp,purification, waterSeal, transferLine
}
export enum ProjectRuralHousing {
  apartment, villa
}

export enum GroundType {
  mountainous, desert
}

export enum ProjectType {
hosing, health, way, knowledge, removeMarginalization,
  social, food, working, industry, water
}

export enum projectType{
  construction , Completion , restoration
}